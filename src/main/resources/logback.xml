<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
<!--        <filter class="ch.qos.logback.classic.filter.LevelFilter">-->
<!--            <level>INFO</level>-->
<!--            <onMatch>ACCEPT</onMatch>-->
<!--            <onMismatch>DENY</onMismatch>-->
<!--        </filter>-->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/logFile.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/logFile.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>5</maxHistory>
            <totalSizeCap>10MB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ru.liga.crud.support.MaskingPatternLayout">
                <maskPattern>['"]?salary['"]?\s*[:=]\s*['"]?(.*?)['",; ]</maskPattern> <!-- Salary pattern -->
                <maskPattern>['"]?telephoneNumber['"]?\s*[:=]\s*['"]?(.*?)['",; ]</maskPattern> <!-- TelephoneNumber pattern -->
                <maskPattern>(\w+@\w+\.\w+)</maskPattern> <!-- Email pattern -->
                <maskPattern>["']?password["']?\s*[:=]\s*["']?(.*?)["',; ]</maskPattern> <!-- Password pattern -->
                <maskPattern>["']?jwtToken["']?\s*[:=]\s*["']?(.*?)["',; ]</maskPattern> <!-- JwtToken pattern -->
                <pattern>%d{HH:mm:ss.SSS}  %-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
            </layout>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>